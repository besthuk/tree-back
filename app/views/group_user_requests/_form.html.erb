<%= form_with(model: group_user_request, local: true) do |form| %>
  <% if group_user_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_user_request.errors.count, "error") %> prohibited this group_user_request from being saved:</h2>

      <ul>
        <% group_user_request.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
