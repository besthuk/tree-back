<%= form_with(model: personal_info, local: true) do |form| %>
  <% if personal_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_info.errors.count, "error") %> prohibited this personal_info from being saved:</h2>

      <ul>
        <% personal_info.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
